package controller;

public class Moving {
	final private Integer LEFT = 37;
	final private Integer UP = 38;
	final private Integer RIGHT = 39;
	final private Integer DOWN = 40;
	final private Integer BACK_SPACE = 8;

	public void movePlayer(Integer cursor) {
		if (cursor == UP) {

		} else if (cursor == DOWN) {

		} else if (cursor == LEFT) {

		} else if (cursor == RIGHT) {

		} else if (cursor == BACK_SPACE) {

		}
	}

	private boolean validateMove(Integer cursor) {

	}
}
